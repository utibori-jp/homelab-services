apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vikunja-tailscale
  namespace: {{ .Values.ingresses.vikunja.namespace }}
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - {{ .Values.ingresses.vikunja.hostname }}
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingresses.vikunja.service.name }}
                port:
                  number: {{ .Values.ingresses.vikunja.service.port }}
