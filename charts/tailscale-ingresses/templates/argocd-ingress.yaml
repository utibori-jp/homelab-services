apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-tailscale
  namespace: {{ .Values.ingresses.argocd.namespace }}
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - {{ .Values.ingresses.argocd.hostname }}
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingresses.argocd.service.name }}
                port:
                  number: {{ .Values.ingresses.argocd.service.port }}
