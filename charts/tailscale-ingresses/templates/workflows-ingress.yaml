apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workflows-tailscale
  namespace: {{ .Values.ingresses.workflows.namespace }}
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - {{ .Values.ingresses.workflows.hostname }}
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingresses.workflows.service.name }}
                port:
                  number: {{ .Values.ingresses.workflows.service.port }}
